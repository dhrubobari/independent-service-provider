import React from 'react';
import { Container, Navbar } from 'react-bootstrap';

const Header = () => {
    return (
        <Navbar bg="white">
    <Container>
      <Navbar.Brand>Adonis and Associates</Navbar.Brand>
    </Container>
  </Navbar>
    );
};

export default Header;